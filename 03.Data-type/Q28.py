#Create a variable called value.

#First assign an integer to it.
#Use type() to identify its type.
#Reassign a string to it.
#Use type() again.
#Explain what changed.


value = 42
print(type(value))  

value = "Now I am text"
print(type(value))  
# Explanation: Python is dynamically typed, meaning a variable's type can change based on the value assigned to it at runtime.